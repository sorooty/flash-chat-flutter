specVersion: "v1"
serviceId: "flash-chat-flutter"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "flash-chat-flutter-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
